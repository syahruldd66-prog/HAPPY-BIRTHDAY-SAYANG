const canvas=document.getElementById("stars"),ctx=canvas.getContext("2d");let W,H,stars=[];function resize(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);W=innerWidth;H=innerHeight;stars=Array.from({length:Math.min(180,Math.floor(W/6))},()=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*1.5+.2,a:Math.random(),s:Math.random()*.015+.003}))}resize();addEventListener("resize",resize);function draw(){ctx.clearRect(0,0,W,H);for(const s of stars){s.a+=s.s;if(s.a>1||s.a<.1)s.s*=-1;ctx.beginPath();ctx.globalAlpha=s.a;ctx.fillStyle="#ffd6e8";ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;requestAnimationFrame(draw)}draw();function spawnHeart(x=Math.random()*100){const h=document.createElement("span");h.className="heart";h.textContent=Math.random()>.25?"♥":"✦";h.style.left=x+"vw";h.style.bottom="-30px";h.style.fontSize=(8+Math.random()*18)+"px";h.style.animationDuration=(5+Math.random()*6)+"s";h.style.opacity=Math.random()*.7+.3;document.getElementById("hearts").appendChild(h);setTimeout(()=>h.remove(),12000)}setInterval(()=>spawnHeart(),650);